# Lenny Environment Configuration
# This file is a placeholder to ensure docker-compose can start.
# Actual values are populated by dev-setup.sh on first container start.
#
# Copy to .env and fill in your values, or run: just dev-setup

# Database (pre-configured for devcontainer)
DATABASE_URL=postgresql+asyncpg://lenny:lenny@db/lenny
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# EVE Online SSO (get credentials from developers.eveonline.com)
EVE_CLIENT_ID=
EVE_CLIENT_SECRET=
EVE_CALLBACK_URL=http://localhost:8000/auth/callback

# LLM Provider
LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.0-flash
OPENAI_API_KEY=
GEMINI_API_KEY=

# Security
SECRET_KEY=dev-placeholder-key-change-in-production

# GitHub (optional)
GITHUB_TOKEN=
GITHUB_USERNAME=
